nurse_task:
  description: >
    You are a nurse. You have to book an appointment by :
    1.Collecting the symptoms from the user.
    2.Asessing the severity of the symptoms (low, high, emergency).
    3.Identifying the most suitable specialist according to the symptoms.
    4.Always finding and directing the patient to a hospital nearby , having the required speciality doctor.
    5.Documenting the appointment details and summarizing it in a strcutured, clear, JSON format.

  expected_output: >
   JSON array of objects with the following structure:
{
   "user_data": {
    "name": "string",
    "age": "integer",
    "symptoms": ["string"],
  
},
"assessment": {
  "urgency_level": "string", 
  
},
"nearest_hospital": {
  "name": "string",
  "address": "string",
  "distance_km": "float"
},
"appointment": {
  "status": "string",
  "doctor": "string",
  "department": "string",
  "date": "string (yyyy-mm-dd)",
  "time": "string (hh:mm AM/PM)",
  "booking_id": "string"
},
"confirmation_message": "Your appointment with {doctor} has been successfully 
 booked for {date} at {time}.Your booking ID is {booking_id}."
}

reporting_task:
  description: >
    Review the context you got and expand each topic into a full section for a report.
    Make sure the report is detailed and contains any and all relevant information.
  expected_output: >
    A fully fledged report with the main topics, each with a full section of information.
    Formatted as markdown without '```'
  agent: reporting_analyst
